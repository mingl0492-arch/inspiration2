# 数据存储说明 v1.0.9

为了兼顾“复制安装目录即可迁移”和“防止安装目录不可写导致数据丢失”，v1.0.9 会同时保存两份数据。

## 主数据位置

```text
应用安装目录/data/inspiration-data.json
```

复制整个安装目录到另一台电脑时，这个文件会一起被复制。

## 备份数据位置

```text
%APPDATA%/灵感管理/inspiration-data.json
```

如果安装目录没有写入权限，应用仍然可以通过这个备份位置保存数据。

## 启动时如何读取

应用启动时会同时检查主数据和备份数据，并自动选择最新/最完整的数据加载。

## 建议

为了实现“复制目录即可迁移”，建议安装到有写入权限的位置，例如：

```text
D:/Apps/灵感管理
C:/Users/你的用户名/Apps/灵感管理
```

不建议安装到：

```text
C:/Program Files
```

因为这个目录通常需要管理员权限。
